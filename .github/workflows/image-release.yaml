name: Build & Release Image

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - v*
jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Login registry
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: Make 
        run: |
              make docker-build
              #make build-chart
      - name: Push 
        run: |
              make docker-push
      - name: Build & Pushing cloudshell image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: docker/Dockerfile-webtty
          labels: |-
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          platforms: linux/amd64,linux/arm64
          #push: ${{ github.event_name != 'pull_request' }}
          build-args: |
            VERSION=${{ steps.get_version.outputs.VERSION }}
          tags: |-
             ghcr.io/cloudtty/cloudshell:${{ steps.get_version.outputs.VERSION }}
      - name: Build & Pushing operator image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: docker/Dockerfile
          labels: |-
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          platforms: linux/amd64,linux/arm64
          #push: ${{ github.event_name != 'pull_request' }}
          build-args: |
            VERSION=${{ steps.get_version.outputs.VERSION }}
          tags: |-
             ghcr.io/cloudtty/cloudshell-operator:${{ steps.get_version.outputs.VERSION }}

